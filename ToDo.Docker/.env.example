# Docker Compose Project Name
# Override the project name (default: ToDo)
# COMPOSE_PROJECT_NAME=ToDo

# SQL Server Configuration
MSSQL_SA_PASSWORD=P@ssword2026
SQL_PORT=1433

# Agent Factory API Configuration
FACTORY_PORT=5000
FACTORY_CONNECTION_STRING=Server=sqlserver;Database=ToDo-db;User Id=sa;Password=P@ssword2026;TrustServerCertificate=True;
# Note: maps to ConnectionStrings__appdb in the container (key read by DependencyInjection.cs)

# Environment
ASPNETCORE_ENVIRONMENT=Development
